@model WebHookApplication.ViewModel.DivideViewModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>


@using (@Html.BeginForm("Divide", "Contact"))
{
    @Html.ListBoxFor(m => m.ListStaffId, new MultiSelectList(Model.ListStaff, "Value", "Text"), new { @class = "form-control", multiple = "multiple", id = "Subjects_dropdown" })

    <br />

    @Html.LabelFor(m => m.KeyWord)
    @Html.TextBoxFor(m => m.KeyWord)

    <br />

    <button>Chia data</button>
}